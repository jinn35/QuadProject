#include <stdio.h>
#include <math.h>

int main()
{
	double a = 5;
	printf("Absolute error looks like this: \n if (fabs(result-expectedResult) < 0.000001 \n\n");
	printf("Relative error looks like this: \n float relativeError = fabs((A-B)/B); \n");

}
